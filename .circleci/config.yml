# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2

jobs:
  build:
    docker:
    # specify the version you desire here
    - image: circleci/openjdk:8-jdk

        # Specify service dependencies here if necessary
        # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

      # Service container image available at `host: localhost`
    - image: circleci/mysql:8.0.11
      environment:
        MYSQL_ALLOW_EMPTY_PASSWORD: yes
        MYSQL_ROOT_PASSWORD: ''
        MYSQL_HOST: 127.0.0.1
        MYSQL_USER: myuser
        MYSQL_PASSWORD: password
        MYSQL_DATABASE: auditdb


    working_directory: ~/audit4jusage

    environment:
      # Customize the JVM maximum heap limit
      MAVEN_OPTS: -Xmx3200m

    steps:
    - checkout
    - run: sudo apt-get install mysql-client --allow-unauthenticated
    - run: mysql -u root -e "create database test1" --protocol=tcp
    - run: mysql -u root -e "create database test2" --protocol=tcp
    - run: mysql -u root -e "create database auditdb2" --protocol=tcp

    - run:
        name: Wait for Database Connection
        # preinstalled in circleci/* docker image
        command: dockerize -wait tcp://localhost:3306 -timeout 1m



    # Download and cache dependencies
    - restore_cache:
        keys:
        - audit4jusage-{{ checksum "pom.xml" }}
        # fallback to using the latest cache if no exact match is found
        - audit4jusage-

    - run: mvn dependency:go-offline

    #- run: mvn clean install

    - run: mvn spring-boot:run

    - save_cache:
        paths:
        - ~/.m2
        key: audit4jusage-{{ checksum "pom.xml" }}

    - store_test_results:
        path: target/surefire-reports